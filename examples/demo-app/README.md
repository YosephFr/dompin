# Demo app

A small static page with a representative variety of DOM patterns (cards, table, form, sidebar, badges) for exercising the DOMPin extension picker.

## Run

```bash
npx serve examples/demo-app
```

or just open `index.html` directly in your browser. The picker works on the `file://` protocol as long as you have enabled the extension on local files in `chrome://extensions`.
